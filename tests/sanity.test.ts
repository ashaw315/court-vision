import { describe, expect, it } from 'vitest';

// Placeholder solely to prove the runner is wired up. It gets deleted once the
// first real transform test lands in Phase 3.
describe('test runner', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
