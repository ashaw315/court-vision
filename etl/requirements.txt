# ETL dependencies. Installed into the repo-root .venv (gitignored):
#   .venv/bin/pip install -r etl/requirements.txt
#
# Stage 1 (transforms) needs only pytest — the transforms are pure functions over
# saved fixtures and make no network calls. nba_api is needed from stage 2 onward,
# when the pipeline actually pulls from stats.nba.com (locally only; the endpoint
# 403s from cloud IPs).

nba_api==1.11.4
pytest==8.3.4
ruff==0.9.2
