[pytest]
# Python tests live in etl/ (the ETL is the only Python in the project). pythonpath
# makes `transforms` importable so the same command works from the repo root, which is
# where every other command in this project is run from.
pythonpath = etl
testpaths = etl/tests
